{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "▶️ Start Structurizr Lite",
      "type": "shell",
      "command": "docker run -it --rm -p 8080:8080 -v \"${workspaceFolder}\":/usr/local/structurizr structurizr/lite && open http://localhost:8080",
      "group": "none",
      "presentation": { "reveal": "always", "panel": "dedicated" },
      "runOptions": { "runOn": "folderOpen" }
    }
  ]
}